
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Initialize the app with the new branding
createRoot(document.getElementById("root")!).render(<App />);
