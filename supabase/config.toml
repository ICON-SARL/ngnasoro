
# The project's ID, can be obtained in the Supabase dashboard
project_id = "xnqysvnychmsockivqhb"

# Controls how tools work in this project
[dev]
  # Enables watch mode for local development
  watch = true

# Configure Edge Functions
[functions]
  [functions.process-client-adhesion]
    verify_jwt = true
  [functions.manage-adhesion-request]
    verify_jwt = true
  [functions.fetch-client-adhesions]
    verify_jwt = true
